# Python Overlay

- Use a supported Python version and declare it.
- Prefer the standard library when it is sufficient.
- Use type hints for maintained interfaces.
- Keep dependencies locked.
- Provide deterministic test and lint commands.
- Avoid executing untrusted serialized data.
