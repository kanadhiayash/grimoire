# Node and TypeScript Overlay

- Declare supported Node versions.
- Commit the appropriate lockfile.
- Use strict TypeScript for maintained code where practical.
- Validate external input at trust boundaries.
- Keep secrets out of client bundles.
- Run lint, type check, tests, and build in CI when configured.
