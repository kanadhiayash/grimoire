# Testing

Tests should protect behavior, not implementation trivia.

## Required

- New behavior receives proportionate test coverage.
- Bug fixes include a regression test when practical.
- Tests must fail for the defect they claim to prevent.
- Flaky tests are defects and must not be normalized.
- Test deletion requires documented justification.

Use the smallest test layer that provides credible evidence, then add broader tests where integration risk requires them.
