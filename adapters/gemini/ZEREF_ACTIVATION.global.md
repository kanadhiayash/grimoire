# Zeref activation

Detect the applicable project context and verified Zeref capability. Defer to the project's Zeref contract only when available. Use `/memory show` or equivalent diagnostics to verify loaded context. Do not claim local runtime or canonical memory from context text alone.
