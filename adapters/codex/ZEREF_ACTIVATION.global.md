## Zeref activation

Before material work, detect whether the current project is Zeref-enabled. When a nearer Zeref `AGENTS.md` applies and runtime capability is verified, defer to it. Otherwise continue under Engineering Standards and report `RUNTIME_PARTIAL`, `INSTRUCTION_ONLY`, `UNAVAILABLE`, or `NOT_VERIFIED` truthfully.
