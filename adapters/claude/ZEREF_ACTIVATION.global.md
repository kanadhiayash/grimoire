# Zeref activation

At session start, verify whether Zeref Memory Engine is available for the current project. If verified, defer to the nearest applicable Zeref `AGENTS.md` and report the activation state. If only instructions are available, report `INSTRUCTION_ONLY`. Never claim runtime execution from an instruction file alone.
