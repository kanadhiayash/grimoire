# Surface Boot

Project: `{{PROJECT_NAME}}`
Surface: `{{SURFACE}}`
Pack version: `{{PACK_VERSION}}`

Read in this exact order:

1. `SOURCES_MANIFEST.json`
2. `AGENTS.md`
3. `PROVIDER_ADAPTER.md`
4. `ZEREF_OPERATIONS_BRIDGE.md`
5. `PROJECT_STATE.md`
6. `MEMORY_STATE.md`

Classify and report exactly one activation state before material work:

`RUNTIME_FULL`, `RUNTIME_PARTIAL`, `PROJECT_SIMULATION`, `CHAT_SIMULATION`, `INSTRUCTION_ONLY`, `UNAVAILABLE`, or `NOT_VERIFIED`.

A browser source pack never proves that the local Zeref runtime or canonical memory writer executed.
